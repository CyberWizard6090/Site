import { useEffect, useState } from "react";
import { useLocation } from "react-router-dom";

export const useUpdateFontAndColorScheme = () => {
    const location = useLocation();
    // const [size, setSize] = useState( parseInt(localStorage.getItem('Special-Fontsize') | "100"))
    // useEffect(() => {


    // }, [location,size]);
    // return {}

}
